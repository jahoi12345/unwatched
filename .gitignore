# build output
dist/
*.tsbuildinfo

# generated ingest data (SQLite db + JSON exports)
ingest/data/

# local FOIA source PDFs/metadata (not ours to redistribute)
ingest/foia-inbox/*
!ingest/foia-inbox/.gitkeep
!ingest/foia-inbox/README.md

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# harness session state (not project config)
.claude/scheduled_tasks.lock

# agent instructions files (kept locally, not published)
AGENTS.md
CLAUDE.md

# jetbrains setting folder
.idea/
